/**
 * The harness entry: Expo Router's, as an Expo app's `main` is. The runtime's
 * install runs before this through Metro (`withWindows`), and Expo Router
 * registers the `main` component the native app asks for.
 */
import 'expo-router/entry';
