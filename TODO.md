# P2P

- Use Evolu for database / auth
- Set custom S3 provider (support multiple)

# ULT DEV KIT

- Vite+ to package?
- Port relevant exo-kit components
- Move UI components to ult.dev/kit
- Make CLI to add/update ui components
  - Determine if project is a monorepo or not
  - Add ui package (configurable name) if monorepo, otherwise a folder
