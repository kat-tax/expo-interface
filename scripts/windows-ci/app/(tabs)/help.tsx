import {Body, Screen, Title} from 'expo-interface';

export default function Help() {
  return (
    <Screen>
      <Title>Help</Title>
      <Body>A footer item of the NavigationView: at the pane’s foot, or the bar’s far end.</Body>
    </Screen>
  );
}
